<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 flex items-center justify-center min-h-screen">

   

    <div class="bg-white p-8 rounded-lg shadow-md w-96">
        <h2 class="text-2xl font-bold text-center mb-6">Login</h2>
        <div class="flex items-center justify-center mb-3">
            <% if (typeof message !== 'undefined' && message) { %>  
                <p class="text-red-700"><%= message %></p>
          <% } %>
            <span
              id="error-message"
              class="text-md text-red-700 "
            ></span>
          </div>
        <form action="/user/login" method="POST" id="login-form">
            <div class="mb-4">
                <label for="email" class="block text-gray-700">Email</label>
                <input type="email" id="email" name="email" required class="border border-gray-300 p-2 w-full rounded focus:outline-none focus:ring focus:ring-indigo-200">
            </div>
            <div class="mb-4">
                <label for="password" class="block text-gray-700">Password</label>
                <input type="password" id="password" name="password" required class="border border-gray-300 p-2 w-full rounded focus:outline-none focus:ring focus:ring-indigo-200">
            </div>
            <div class="mb-6">
                <button type="submit"  class="w-full bg-indigo-600 text-white font-semibold py-2 rounded hover:bg-indigo-700 transition duration-200">Login</button>
            </div>
        </form>
        <div class="mt-5 flex justify-center mb-5 w-ful">
            <a href="/auth/google">
                <button class="flex items-center justify-center w-full px-4 py-2 bg-white border border-gray-300 text-gray-600 rounded-md hover:bg-gray-100 transition duration-300">
                       <img src="https://img.icons8.com/color/48/000000/google-logo.png" alt="Google" class="h-5 w-5 mr-2">
                       Sign up with Google
                    </button>
                </a> 
        </div>
        <p class="text-center text-gray-600">
            Don't have an account? 
            <a href="/user/signup" class="text-indigo-600 hover:underline">Sign up</a>
        </p>
        <p class="text-center text-gray-600 mt-2">
            <a href="/user/forgotPassword" class="text-indigo-600 hover:underline">Forgot Password?</a>
        </p>
    </div>
    
   
    <script src="/javascript/userLogin.js"></script>
</body>
</html>
