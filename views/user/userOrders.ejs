<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Address Page</title>
    <script src="https://cdn.tailwindcss.com"></script>

    <link
      href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-100">
    <!-- Header  -->
    <header class="bg-stone-950 py-5 px-4 sm:px-8 shadow-md">
      <div class="container flex justify-between items-center">
        <!-- nav bar logo -->
        <div class="logo">
          <a href="/user/home">
            <h1 class="text-2xl sm:text-3xl text-white font-bold uppercase">UrbanGents</h1>
          </a>
        </div>
        
            <!-- Navbar search input -->
        <div class="relative hidden md:block">
            <input type="text" id="search-input"
            class="border border-gray-300 w-80 rounded-lg px-2 py-2 focus:outline-none focus:ring-2 focus:ring-gray-500 "
            placeholder="Search...">
            <button id="search-button">
              <svg class="w-5 h-5 absolute right-3 top-3 text-gray-400" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="11" cy="11" r="8"></circle>
                <line x1="21" y1="21" x2="17.65" y2="17.65"></line>
              </svg>
            </button>
        </div>
      
         <!-- Desktop Menu -->
        <div class="hidden sm:flex justify-between items-center space-x-6">
          <a href="/user/profile" class="text-white">
            <span class="material-icons">person</span>
          </a>
          <a href="/user/cart" class="text-white mx-4">
            <span class="material-icons">shopping_cart</span>
          </a>
          <a href="/user/profile/wishlist" class="text-white">
            <span class="material-icons">favorite</span>
          </a>
        </div>
  
        <!-- Mobile Menu Toggle -->
        <div class="sm:hidden flex items-center space-x-4" id="menu-toggle">
          <button class="text-white focus:outline-none">
            <span class="material-icons">menu</span>
          </button>
        </div>
  
        <!-- Mobile Sidebar -->
        <div id="mobile-sidebar" class="fixed inset-0 bg-gray-800 bg-opacity-100 z-50 hidden transform -translate-x-full transition-transform duration-300">
          <div class="flex flex-col items-center justify-center space-y-6 p-6">
            <a href="/user/cart" class="text-white text-xl">Cart</a>
            <a href="/user/profile/wishlist" class="text-white text-xl">Wishlist</a>
            <a href="/user/profile" class="text-white text-xl">Profile</a>
            <button id="close-sidebar" class="text-white text-2xl">&times; Close</button>
          </div>
        </div>
  
      </div>
    </header>

    <!-- Main Container -->

    <div class="container absolute mx-auto mt-3 p-4 flex flex-col lg:flex-row sticky top-4">
      <!-- Sidebar -->
      <% if(typeof user !== 'undefined' && user) { %>
      <aside
        class="w-full lg:w-1/6 bg-white p-4 rounded-lg shadow-md mb-4 lg:mb-0"
      >

        <div class="text-center">
          <div class="w-24 h-24 mx-auto rounded-full bg-gray-300 mb-2">
            <img
              src="https://avatar.iran.liara.run/public/boy?username=Ash"
              alt="User Avatar"
              class="w-full h-full rounded-full object-cover"
            />
        </div>

            <h2 class="text-lg font-semibold uppercase"><%=user.firstName %></h2>
        </div>
        <% } %> 
        <!-- Navigation Menu -->
        <nav class="text-center mt-5 space-y-4">
          <a
            href="/user/profile"
            class="flex items-center justify-center text-gray-700 bg-gray-200 p-2 rounded"
            aria-label="Person Info"
          >
            <span class="material-icons text-lg leading-none">person</span>
            <span class="ml-2">Person Info</span>
          </a>
       
          <a
            href="/user/profile/address"
            class="flex items-center justify-center text-gray-700 bg-gray-200 p-2 rounded"
            aria-label="Person Info"
          >
            <span class="material-icons text-lg leading-none"> home </span>
            <span class="ml-2">Address</span>
          </a>
          <a
            href="/user/profile/wishlist"
            class="flex items-center justify-center text-gray-700 bg-gray-200 p-2 rounded"
            aria-label="Wishlist"
          >
            <span class="material-icons text-lg leading-none">favorite</span>
            <span class="ml-2">Wishlist</span>
          </a>
          <a
            href="/user/profile/orders"
            class="flex items-center justify-center text-gray-700 bg-gray-200 p-2 rounded"
            aria-label="Orders"
          >
            <span class="material-icons text-lg leading-none">inventory_2</span>
            <span class="ml-2">Orders</span>
          </a>
          <a
            href="/user/profile/wallet"
            class="flex items-center justify-center text-gray-700 bg-gray-200 p-2 rounded"
            aria-label="Wallet"
          >
            <span class="material-icons text-lg leading-none"
              >account_balance_wallet</span
            >
            <span class="ml-2">Wallet</span>
          </a>
         
          <a
            href="/user/logout"
            class="flex items-center justify-center text-red-600 bg-grey-100 p-2 rounded mt-10"
            aria-label="Logout"
          >
            <span class="material-icons text-lg leading-none">logout</span>
            <span class="ml-2">Logout</span>
          </a>
        </nav>

      </aside>


      <!-- Profile Details -->
      <main class="w-full lg:w-3/4 lg:ml-4 bg-white px-6 pb-5 rounded-lg shadow-md">
        <span href="/user/home" class="text-black mt-5"
        ><a href="/user/home">Home</a> >
        <a href="/user/profile/orders"><strong>Orders</strong></a>
      </span>
        <div class="max-w-7xl mx-auto px-8">
          <h1 class="text-3xl font-semibold text-center mb-6">Your Orders</h1>
      
          <% if (typeof orders !== 'undefined' && orders.length > 0) { %>
            <% orders.forEach(order => { %>
                <% order.items.forEach(item => { %>
            <!-- Order Items in Grid -->
          <div id="order-items" class="grid grid-cols-1 gap-4 sm:grid-cols-1 lg:grid-cols-1 my-4">
            <!-- Order Item -->
            <div class="order-item bg-white shadow-lg rounded-lg p-4 flex flex-col sm:flex-row items-start sm:items-center justify-between">
              <div class="flex items-start sm:items-center space-x-4 w-full">
                <!-- Product Image -->
                <img
                  src="<%= item.productId.images[0] %>"
                  alt="Product Image"
                  class="w-16 h-16 sm:w-20 sm:h-20 object-cover rounded-md flex-shrink-0"
                />
                <!-- Order Info -->
                <div class="order-info space-y-2 w-full">
                  <h3 class="text-sm sm:text-lg font-semibold text-gray-800 truncate"><%= item.productId.productName %></h3>
                  <p class="text-xs sm:text-sm text-gray-500">Order Date: <%= order.createdAt.toDateString() %></p>
                  <p class="text-xs sm:text-sm text-gray-600">Quantity: <%= item.quantity %></p>
                  <p class="text-xs sm:text-sm text-gray-600">Total Price: <%= item.subTotal %></p>
                  <% if (item.status === 'pending' || item.status === 'shipped') { %>
                    <p class="mt-2">
                      <span class="inline-block px-2 sm:px-3 py-1 text-xs sm:text-sm font-medium rounded bg-yellow-100 text-yellow-800 uppercase">
                        <%= item.status %>
                      </span>
                    </p>
                  <% } else if (item.status === 'cancelled' || item.status === 'returned') { %>
                    <p class="mt-2">
                      <span class="inline-block px-2 sm:px-3 py-1 text-xs sm:text-sm font-medium rounded bg-red-200 text-red-800 uppercase">
                        <%= item.status %>
                      </span>
                    </p>
                  <% } else { %>
                    <p class="mt-2">
                      <span class="inline-block px-2 sm:px-3 py-1 text-xs sm:text-sm font-medium rounded bg-green-100 text-green-800 uppercase">
                        <%= item.status %>
                      </span>
                    </p>
                  <% } %>
                </div>
              </div>
              <!-- Button Section -->
              <div class="flex flex-col sm:flex-row sm:space-x-4 mt-4 sm:mt-0 w-full justify-end">
                <% if (item.status === 'delivered') { %>
                  <button class="return-button bg-red-500 text-black px-4 py-1 text-sm rounded-md hover:bg-red-600 w-full sm:w-auto" data-itemId="<%= item._id %>" data-orderId="<%= order._id %>">Return</button>
                <% } else if (item.status !== 'returned' && item.status !== 'delivered' && item.status !== 'cancelled') { %>
                  <button class="cancel-button bg-gray-300 text-black px-4 py-2 text-sm rounded-md hover:bg-gray-400 w-full sm:w-auto" data-itemId="<%= item._id %>" data-orderId="<%= order._id %>">Cancel</button>
                <% } %>
                <button class="view-details bg-black text-white px-4 py-2 text-sm rounded-md hover:bg-gray-800 w-full sm:w-auto mt-2 sm:mt-0" data-orderId="<%= order._id %>">View Details</button>
              </div>
            </div>
          </div>

                <% }) %>
            <% }) %>
          <% } else { %>
            <p class="text-center text-gray-600">No orders found.</p>
          <% } %>
        </div>
      </main>
      
    </div>
  </body>

  <script src="/javascript/userOrders.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
  <!-- Include SweetAlert2 from a CDN -->
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</html>
